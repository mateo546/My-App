apiVersion: v1
kind: Secret
metadata:
  name: mateo-ingress-tls
  namespace: mateo-pokeapp-dev
type: kubernetes.io/tls
data:
  tls.key: ${{ secrets.POKEAPP_KEY }}
  tls.crt: ${{ secrets.POKEAPP_CRT }}
